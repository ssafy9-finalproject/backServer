<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.edu.review.model.dao.ReviewMapper">

	<select id="planList" parameterType="String"
		resultType="myPlanResponseDto">
		select plan_id, member_id, title, start_date, end_date, contents
		from
		plan
		where member_id = #{id} and status = 'Y'
	</select>
	
	<!-- select p.plan_id, member_id, title, start_date, end_date, contents, plan_attraction_id, content_id, plan_date
from plan p
inner join plan_attraction a on p.plan_id = a.plan_id
where p.plan_id = 30
order by plan_attraction_id ASC, plan_date ASC; -->

</mapper>
